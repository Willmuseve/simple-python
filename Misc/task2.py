# A program that converts seconds to minutes and hours.
# present the minutes as integers and hrs as decimal values

seconds = 10400
minutes = seconds // 60
hours = minutes / 60

print(seconds, "is equivalent to:")
print(minutes, "minutes")
print(hours, "hours")